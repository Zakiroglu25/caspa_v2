import 'package:flutter/material.dart';

class NewsElement extends StatelessWidget {

  String? title;
  String? asset;
  Function? onTap;

  NewsElement({required this.title,required this.asset,this.onTap});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
